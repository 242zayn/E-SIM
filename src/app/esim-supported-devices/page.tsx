import React from 'react';

import DeviceCompatibility from '@/components/layout/DeviceCompatibility';

const page = () => {
    return (
        <div>
            <DeviceCompatibility />
        </div>
    );
};

export default page;
